name: infogames
description: Info about videogames.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter_bloc: ^8.0.0
  json_serializable: ^6.1.1
  json_annotation: ^4.4.0
  http: ^0.13.4
  flutter_dotenv: ^5.0.2
  cached_network_image: ^3.2.0
  flutter_rating_bar: ^4.0.0
  mocktail: ^0.2.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.5
  very_good_analysis: ^2.4.0
  bloc_test: ^9.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/.env

